/*  
* 问题定位：  
你在使用 break 语句时，确实会跳出 for 循环，  
* 但问题在于 if (needleCharArray[slow] != haystackCharArray[i]) 这一段依然会被执行。  
为什么会发生这种情况？  
break 语句会跳出 当前 的 for 循环，但并 不会立即停止方法的执行。  
也就是说，跳出 for 循环后，程序还是会继续执行 for 循环后面的代码。  
在你的代码中，if (needleCharArray[slow] != haystackCharArray[i]) 紧接着 for 循环，  
* 所以它会在跳出循环后立即执行。即使你已经完全匹配了 needle，在 break 后，这个判断语句仍然会执行，导致 -1 被返回。  
问题的根本原因：  
当 slow == needle.length() 时，你已经找到了完整匹配并使用 break 跳出循环，  
* 但 代码继续执行 if (needleCharArray[slow] != haystackCharArray[i]) 语句，这会导致错误的 -1 返回。  
* */